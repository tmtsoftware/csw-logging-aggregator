filebeat:
    prospectors:
        - 
            type: log
            enabled: true
            paths:
                - 
                    /watch/*.log
                    #/tmp/csw/logs/*.log

output:
    logstash:
        hosts: ["logstash:5044"]